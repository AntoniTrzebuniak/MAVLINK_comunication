# tests

Contains functional and integration tests related to MAVLink and mission validation.

## Files
- `checkprearmtest.py`, `loopback.py`, `mpt.py`, `test.py`, `zasieg.py`: various test scripts.

## Usage
- Run tests with `python3 tests/<script>.py`.
- Ensure MAVLink device is connected and config is valid before running hardware tests.
