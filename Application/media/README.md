# media

Storage for mission generated media and log outputs.

## Structure
- `logs/`: runtime logs from system.
- `photos/`: captured images organized by mission.
- `videos/`: video files from camera capture (if implemented).

## Usage
- The code writes to these folders automatically when capturing and logging.
- Do not edit manually unless cleaning old mission data.
