# Logger

Logging utilities for diagnostics and mission events.

## Files
- `log_module.py`: provides logging wrapper functions.

## Usage
- Import from `Application.Logger.log_module` and log states/events during mission.

Example:

``` {python}
self.logger = get_logger(__name__)
```

